# Lazy Loading

Also called Asynchronous loading, it's an optimisation technique for the online content. Instead of loading the whole content and rendering it to the user, the lazy loading will only render the necessary sections of the page and will delay the sections that are not needed at the moment.

